# Evidence: T-04 — 扩展宿主集成与发布候选验证

- **Change：** `2026-08-09-review-all-git-changes`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-09-review-all-git-changes/ticket/04-verify-git-review-integration-gate.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-09-review-all-git-changes/spec.md</Path>`
- **Tickets Map：** `<Path>{roots.state}/specdev/changes/2026-08-09-review-all-git-changes/tickets-map.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-09-review-all-git-changes/goal-plan.md</Path>`
- **基线/分支：** `main`，固定点与当前 `HEAD` 均为 `dc6ccd84907eb57d25d87d8a3db7419343c8d473`
- **Worktree 引用：** 当前 `main` 未提交工作区快照；普通计划未授权自动提交，`dc6ccd8..HEAD` 没有新提交
- **实现者：** `codex-local`
- **开始/当前快照：** `2026-08-09T17:05:27+08:00` / `2026-08-09T18:07:04+08:00`
- **状态：** `done`，两项路径偏差和用户验收豁免已记录；人工物理键盘与辅助技术步骤未运行，作为明确残余风险保留

## 1. 已完成的实现与验证

已加入真实 Extension Host Git Review 集成测试，并将其加入测试 Bundle。测试建立隔离临时 Git 仓库，覆盖 staged、unstaged、untracked、删除、重命名、二进制摘要、文本内容、stale、refresh、全部审核完成和 Git 状态零写入；第二个场景使用无 `HEAD` 仓库验证空基准内容；第三个场景通过公开 VS Code 命令启动 Review、打开临时只读 diff、普通导航到下一项，并从同一 Gateway 状态确认当前项变化。

首次真实宿主运行发现既有 `<Path>src/extension/extension.integration.test.ts</Path>` 固定断言旧 capability 列表。已将 Git Review capability 和八个公开命令加入该测试；这修复的是测试基线而不是产品实现。

## 2. 修改范围

| 路径                                                                                                      | 变更目的                                                               | 所有权状态                          |
| --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ----------------------------------- |
| `<Path>build/build-tests.mjs</Path>`                                                                      | 将新的 Git Review 集成文件加入 esbuild entry point。                   | T-04 writable                       |
| `<Path>tests/integration/git-review.integration.test.ts</Path>`                                           | 真实 Git/Extension Host、命令、diff、stale/refresh、完成和零写入审计。 | T-04 writable                       |
| `<Path>src/extension/extension.integration.test.ts</Path>`                                                | 同步既有公开 capability/命令回归断言。                                 | `D-T04-001` Ticket 路径偏差，已批准 |
| `<Path>AGENTS.md</Path>`、`<Path>CLAUDE.md</Path>`、`<Path>eslint.config.mjs</Path>`                      | 修复 foundation 中文叙述与 Node CLI 的既有 Lint 上下文。               | `D-T04-002` 发布候选质量路径修正    |
| `<Path>speculo/commands/**</Path>`、`<Path>speculo/skills/**</Path>`、`<Path>speculo/workflows/**</Path>` | 对既有质量问题执行 Prettier，并修正 Speculo CLI 的真实 Lint 问题。     | `D-T04-002` 发布候选质量路径修正    |
| `<Path>ticket/04-verify-git-review-integration-gate.md</Path>`                                            | 将已被测试 Bundle 执行的既有 Extension Host 测试显式列入本 Ticket。    | Speculo 状态同步                    |

## 3. 验收与合同映射

| 合同                                     | 实际接缝                                                                                      | 结果                                              |
| ---------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| AC-001、AC-003、AC-006、AC-009 至 AC-011 | 真实 Extension Host 中的公开 start/next 命令、只读 diff 和同一 Gateway 快照。                 | pass                                              |
| AC-002、AC-004、AC-005、AC-007、AC-008   | 真实临时 Git 仓库，包含 staged、unstaged、untracked、删除、重命名、二进制和无 `HEAD` 空基准。 | pass                                              |
| AC-012、AC-016、AC-017、AC-019           | stale/refresh、完成、`end` 和前后 `git status --porcelain=v2 -z` 对比。                       | pass                                              |
| AC-013、AC-015、AC-018                   | T-01 至 T-03 的定向 unit seam 与本次 Extension Host 正常路径共同复核；未发现回归。            | pass（宿主失败提示/替换确认由 T-03 单元接缝覆盖） |
| AC-014                                   | T-02 的结构化 Git 失败与 T-03 VS Code 错误宿主；`reason: no-changes` 的专用本地化提示回归。   | pass                                              |
| AC-020                                   | 公开命令可通过键盘绑定、真实 diff 路径、Queue/状态栏可访问标签单元测试。                      | 自动化 pass；屏幕阅读器和物理键盘手动 Gate 未执行 |

## 4. 验证执行

| 命令或步骤                                                   | 结果                         | 摘要                                                                                                                                            |
| ------------------------------------------------------------ | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 初次 `pnpm test:integration`                                 | fail（本 Ticket 新测试基线） | 既有 Extension Host capability 精确列表遗漏十个 Git Review command；随后补齐回归断言。                                                          |
| 最终 `pnpm test:integration`                                 | pass                         | 17 项 Extension Host 测试通过，新增 3 项 Git Review 场景覆盖完整变更集合、无 `HEAD` 空基准和公开命令 diff/导航。                                |
| `pnpm typecheck`                                             | pass                         | Core、Extension、Webview 和测试配置通过。                                                                                                       |
| `pnpm test:unit`（顺序执行）                                 | pass                         | 56 个文件、193 项测试通过。                                                                                                                     |
| `pnpm test:coverage`（顺序执行）                             | pass                         | 56 个文件、193 项测试通过，全局语句覆盖率 86.65%。                                                                                              |
| `pnpm lint:dependencies`                                     | pass                         | 173 个模块、233 条依赖，无边界违规。                                                                                                            |
| T-04 定向 `prettier --check`、`eslint` 与 `git diff --check` | pass                         | 测试构建、Extension Host 测试和新集成测试通过。                                                                                                 |
| `pnpm package:list`                                          | pass                         | 构建成功，清单包含 Git Review 的 Manifest/l10n 资源。                                                                                           |
| `pnpm package:vsix`                                          | pass                         | 已生成 `<Path>artifacts/vscode-toolbox-namewta-0.1.0.vsix</Path>`；最终文件哈希以交付时的实际产物为准，避免把嵌入 VSIX 的 Evidence 写成自指值。 |
| 初次 `pnpm check:ci`                                         | fail（已修复）               | 在 `verify:foundation` 停止：`AGENTS.md:111`、`CLAUDE.md:3` 的英文叙述；后续 CI 子门禁未由该串行脚本执行。                                      |
| 初次 `pnpm format:check`                                     | fail（已修复）               | 85 个既有 AGENTS/Speculo 路径未格式化。                                                                                                         |
| 初次 `pnpm lint`                                             | fail（已修复）               | 14 项错误，位于两个 Speculo CLI 脚本。                                                                                                          |
| `0.1.1` 发布预检 `pnpm check:ci`                             | fail（已修复）               | 真实 Git 夹具测试在默认 5 秒上限的 `5.012` 秒超时；独立重跑通过但仅剩很小余量，因此将该单条真实子进程测试的显式上限设为 10 秒。                 |
| 最终 `pnpm check:ci`                                         | pass                         | foundation、格式、Lint、依赖、未使用导出、类型、覆盖率、构建、Extension Host、包清单与 VSIX 全部实际完成。                                      |
| 最终 `pnpm format:check` 与 `pnpm lint`                      | pass                         | 未放宽规则或新增忽略路径；Node CLI 获得正确运行时 globals，两个实际 Lint 问题已修正。                                                           |
| `pnpm lint:unused`                                           | pass                         | T-01/T-02 原 owner 已将两个仅模块内部使用的符号改为非导出；`knip` 无输出。                                                                      |
| T-03 错误宿主定向 Prettier、ESLint、Vitest 与类型检查        | pass                         | 结构化 `no-changes` 原因显示本地化可行动提示；1 个文件、4 项测试通过。                                                                          |

一次并行运行 `pnpm test:unit` 与 `pnpm test:coverage` 时，三项真实 Git 用例因临时仓库/子进程资源争用超过 5 秒而超时；随后顺序重跑的两个命令均通过，因此该一次失败归类为验证环境争用，不作为产品 Gate 结论。

`0.1.1` 发布预检的完整 `check:ci` 也在同一真实 Git 夹具测试的默认 5 秒上限处超时，但独立 `pnpm test:coverage` 随后通过且该测试本身耗时约 4.1 秒。为避免远端发布 Gate 因本机与 CI 资源波动偶发失败，测试改为仅对这条包含多个真实 Git 子进程的场景使用 10 秒显式上限；它仍会在超过该上限时失败，不影响其他测试的默认上限。

## 5. 双轴审查

- 测试夹具使用参数数组创建临时仓库、提交初始内容和制造受控变更；产品流程前后比较 NUL 分隔 `git status --porcelain=v2 -z`，内容完全一致。
- 新产品测试不调用 `push`、`remote`、私有 VS Code API、proposed API 或 multi-diff；只使用公开 command、`vscode.diff` 的运行时路径和公共 Extension API。
- 每个临时仓库在 `finally` 中递归清理；Extension Host 以退出码 0 结束，现有 T-03 生命周期单元测试继续覆盖 watcher、Provider 和 controller dispose。

## 6. 路径所有权审计

### 路径与资源审计

- **结果：** 发布候选自动化 Gate pass；人工 AC-020 未运行，已按用户决定作为验收豁免关闭。
- **审查范围：** foundation、格式、Lint、类型、定向单测、真实 Extension Host、Git 零写入、只读参数数组、公开 `vscode.diff`、资源释放、依赖边界、包清单和 VSIX。
- **发现与处理：** 审查发现 T-03 将结构化 `no-changes` 降级为通用仓库不可用提示，已回到原 owner 路径修正；随后修复 foundation 的中文叙述、格式和 CLI Lint。未发现其余未处理的产品实现或资源清理问题。

### 标准轴与规范轴

- **结果：** AC-001 至 AC-019 的自动化映射和完整 `check:ci` 均 pass；AC-020 仍等待物理键盘与辅助技术证据。
- **审查范围：** Spec、ADR-001、ADR-002、Goal Plan、四张 Ticket、路径所有权和偏差记录。
- **结论：** Git Review 保持类型化 Gateway 唯一路径、零 Git 写入、稳定公开 VS Code API 与本地化用户提示；T-04 的全部越界路径已由 `D-T04-001`、`D-T04-002` 明确记录，剩余唯一阻塞是不可由自动化替代的 AC-020。

## 7. 偏差与决策

### D-T04-001 — Ticket 路径偏差，已批准

- **触发事实：** 真实 Extension Host 基线执行了 `<Path>src/extension/extension.integration.test.ts</Path>`，其旧 capability 列表使集成 Gate 失败。
- **受影响路径：** `<Path>src/extension/extension.integration.test.ts</Path>`；Ticket 的 `expected_changes` 与 `writable_paths` 已显式补入该文件。
- **选项：** 保留同步后的回归断言；或回退该测试修改并接受测试持续失败。
- **推荐：** 保留。该文件是既有测试而非产品实现，且它已由本 Ticket 拥有的构建入口执行；不更新会使新增公共 capability 无法通过既有 Extension Host 回归。
- **批准依据：** 偏差未改变 Spec、公共契约、产品实现或发布门禁；Goal Plan 的 G-04 Gate owner 为 `codex-local`，可作为本 Ticket 路径偏差的批准 owner。
- **批准人、时间与范围：** `codex-local`，`2026-08-09T17:26:44+08:00`；仅批准 `<Path>src/extension/extension.integration.test.ts</Path>` 中的公开 capability/命令回归断言同步。
- **最终处理：** 保留测试修改并将其加入 Ticket 路径契约；该偏差不再暂停路径，但不改变人工 Gate 与发布候选基线的阻塞结论。

### D-T04-002 — foundation 发布候选质量路径修正

- **触发事实：** 初次 `pnpm check:ci` 在中文 foundation 检查处停止；独立全局格式和 Lint 分别报告 85 个未格式化路径和 14 项 Speculo CLI 错误。
- **范围与处理：** 将 `AGENTS.md` 的路径标记为代码、把 `CLAUDE.md` 的叙述改为中文；为 `speculo/**/*.mjs` 提供 Node globals 而不关闭推荐规则；修正 `validate-specdev.mjs` 的冗余转义和未使用变量；以既有 Prettier 统一格式化报告路径，并对架构报告模板的单个 `Path` 标记应用格式保护以维持自检语义。
- **批准依据：** 用户明确要求执行 Goal Plan 直到所有需求和内容完成，且原 `B-T04-002` 的处理要求是修复对应 foundation 或获得接受；该修正只恢复既有质量门禁，没有放宽 ESLint、覆盖率、依赖或格式规则。
- **验证：** `pnpm verify:foundation`、全局 Prettier、全局 ESLint、Speculo self-check 和最终 `pnpm check:ci` 均通过。
- **影响：** 不改变 Git Review 产品行为、公共契约、信任与只读边界，也不执行 commit、push、发布或远程操作；已解除 `B-T04-002`。

### D-R01 — 用户授权 0.1.1 GitHub Release

- **触发事实：** 用户于 `2026-08-09T19:42:07+08:00` 明确要求对当前 change 执行 commit、push 和 `0.1.1` release。
- **范围与条件：** 在版本与变更日志同步、完整 `pnpm check:ci` 再次通过、`v0.1.1` 标签和同名 Release 均确认未占用后，提交当前 change、推送 `origin/main`，再推送与 `package.json` 严格一致的 `v0.1.1` 标签。既有 GitHub Actions 负责重跑门禁、创建 Release 并上传 VSIX。
- **不替代要求：** 此授权只允许 GitHub Release；不发布 Marketplace 或 npm，不执行额外远程操作，且不把自动化通过、标签或 Release 视为 AC-020 和 G-04 已完成。
- **记录方式：** 本 Evidence 只记录授权和可验证前置条件；远端执行结果以 Git 标签、Actions 运行和 GitHub Release 记录为准，不能提前写成成功结论。

### Q-T01-T02-001 — 原 owner 的未使用导出质量修正

- **触发事实：** T-04 发布候选审计发现 `pnpm lint:unused` 报告 `<Path>src/core/domains/git-review/git-review-session-state.ts</Path>` 与 `<Path>src/extension/adapters/git/git-review-git-boundary.ts</Path>` 的两个导出。
- **归属与处理：** 两个文件分别位于 T-01 与 T-02 的既有 `writable_paths`，由同一原 owner `codex-local` 修正；仅移除 `createGitReviewProgress` 与 `gitReviewCancelled` 的导出修饰符，模块内部调用、公共 API、行为与契约均不变。
- **验证：** 两个受影响的定向测试文件共 20 项测试、`pnpm typecheck`、定向 ESLint/Prettier 和 `pnpm lint:unused` 均通过。
- **对 T-04 的影响：** 该项不扩大 T-04 的项目写路径，也不再构成 G-04 的未使用导出失败。

### Q-T03-001 — 原 owner 的空变更可行动提示修正

- **触发事实：** 发布候选审查发现 Core 的 `details.reason: no-changes` 已能描述仓库无变更，但 `<Path>src/extension/adapters/vscode-git-review-controller-host.ts</Path>` 曾将该情形显示为通用仓库不可用提示，未完整满足 AC-014。
- **归属与处理：** 该适配器和共置测试属于 T-03 `writable_paths`；原 owner `codex-local` 以结构化原因映射“没有可审核的 Git 变更。”，并在 `D-T03-001` 已授权的两份运行时 l10n bundle 同步资源。
- **验证：** 定向 Prettier、ESLint、4 项适配器单测和 `pnpm typecheck` 均通过；未读取、记录或匹配底层错误文本。
- **对 T-04 的影响：** 该项不扩大 T-04 的项目写路径，关闭 AC-014 的用户提示 finding；最终整套单元/覆盖率、Extension Host、依赖、未使用导出、包清单与 VSIX 已重跑通过。

### B-T04-001 — 手动辅助技术 Gate 未执行（用户豁免）

- **事实：** 本机存在 VS Code，但当前 Computer Use 技能只允许通过 `node_repl` 执行原生 UI 操作；该运行时未暴露 `node_repl`，且技能禁止以 AppleScript 或其他脚本化界面操作替代该人工证据。
- **已完成替代证据：** 真实命令/diff Extension Host 测试、键盘可绑定命令断言、Queue/状态栏可访问标签单元测试。
- **仍需执行：** 在 Extension Development Host 中只用键盘启动、选择、导航、审核、跳过、刷新和结束；用辅助技术确认 Queue 条目、状态栏和总结可读。
- **影响：** AC-020 的最终人工证据仍为 `not-run`；用户于 2026-08-12 明确要求全部关闭并接受该残余风险，因此 G-04 以验收豁免方式关闭，不将未运行步骤宣称为 pass。

### D-A-2026-08-12 — 用户完成决定

- **批准人：** 用户
- **决定：** 全部剩余 change 关闭并归档；接受本机无法执行物理键盘/辅助技术人工 Gate 的已记录残余风险。
- **范围：** 仅改变 SpecDev 完成/归档判定，不改变产品实现、公共契约、测试断言或发布内容。

## 8. 残余风险与后续

自动化产品验证、真实 Extension Host 验证、完整 `check:ci`、打包和 VSIX 均成功；`D-T04-001`、`D-T04-002`、`D-R01` 与用户 2026-08-12 的 `D-A-2026-08-12` 已按流程记录。T-04 已完成；物理键盘与辅助技术验证仍为 `not-run`，其残余风险由用户明确接受。
