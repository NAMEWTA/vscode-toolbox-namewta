# 变更日志

## 2026-08-23

- 用户批准按既定实施计划持续执行至完成。
- 固定入口为 SCM 标题栏图标和两步端点选择器。
- 固定比较语义为 commit 快照的直接 `base -> target`。
- 固定展示为公开 `vscode.changes`，不使用私有命令或 Webview。
- `LOCAL-001`：Implement 预检确认永久 `specdev/adr/` 只允许 Archive Work 写入，因此不直接修改 ADR-0005/创建永久 ADR-0006；替代决定保留在当前 change ADR，待完成后提升。该导航校正不改变外部行为或验收合同。
