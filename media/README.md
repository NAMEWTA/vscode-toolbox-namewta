# 品牌资源

`icon.svg` 是项目 logo 的可缩放源文件，表达工具箱、代码括号和 Git 审核时间线三个核心意象。`icon.png` 是从同一源文件导出的 128×128 发布图标，由 `package.json` 的 `icon` 字段引用。

SVG 仅作为源码和 README 预览使用；VS Code 扩展清单使用 PNG，符合 `vsce` 对扩展图标的格式要求。两个文件均为本仓库自有资源，不包含外部可执行内容。
