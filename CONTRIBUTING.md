# 贡献指南

## 环境准备

```bash
corepack enable
pnpm install
pnpm dev
```

开发环境使用 Node.js 22 LTS，并使用 `package.json` 中固定的 pnpm 版本。

## 修改代码前

阅读 `AGENTS.md`、`docs/rules/` 中相关规范和架构 ADR。必须保持 Core、扩展宿主与 Webview 的运行边界。

## 拉取请求要求

- 每次修改只解决一个明确目标。
- 使用具体文件名和职责名，禁止创建通用辅助收容目录或文件。
- 新行为和缺陷修复必须补充测试。
- 所有源码注释、JSDoc 和开发文档必须使用中文。
- 用户可见文案必须通过适当的 VS Code 国际化机制维护。
- 公共行为或架构变化必须同步更新文档和 ADR。
- 禁止提交秘密、生成缓存、覆盖率目录、本地配置档案或 VSIX 文件。

日常修改运行 `pnpm check`。涉及 VS Code 行为或发布流程时运行 `pnpm check:ci`。

## 提交建议

提交信息应简洁、明确并使用祈使语气。条件允许时，将纯格式化修改与行为修改分开。不得仅因个人风格偏好重写无关代码。

## 第三方代码

引入或改写第三方源码前，必须确认许可证兼容性，并更新 `THIRD_PARTY_NOTICES.md`。不得删除上游版权声明或许可证要求。
